An interaction suite becomes useful to a maintainer when its coverage intent, execution shape, and boundaries can be read together. In this change, the focused command `test:e2e:web:cross-browser` selects the suite, the configuration declares projects `chromium`, `firefox`, `mobile-chromium`, and `webkit`, the test policy records `screenshot=only-on-failure` and `retries: 0`, the workflow invokes `test:e2e:web:visual` and `test:e2e:web`, and the static cases name scenarios such as “Dropdown supports keyboard navigation, focus restore, portal, outside click, and positioning” and “Modal renders in a portal, traps focus, and closes with Escape”. That structure gives the reader a way to follow the engineering work from a command, through configuration, to the cases the command is meant to organize. It also defines a necessary limit: declarations show what the repository has arranged to inspect, while a successful run requires execution evidence. Keeping that distinction visible makes the article more useful than a list of scenario names. It explains which maintenance question each surface answers, how a later edit could make the surfaces drift apart, and where further verification begins. The result stays specific without treating a configured project or a static test title as proof that an interaction works at runtime.

## What changed

The change gives the focused suite a named entrypoint and a visible execution shape. The package scripts expose a focused test command; the configuration declares the selected projects; and the test policy records explicit controls. Those pieces answer different maintenance questions, so keeping them together is more useful than repeating a list of files. The static cases cover the represented interaction scenarios, which makes the intended review boundary inspectable. The article uses the declarations to explain how the suite is organized and where a contributor should look when the selection changes. It does not turn a case title or a configured project into proof that a run passed.

## How it works

The arrangement works as a chain rather than as independent test metadata. A named command selects the suite, the project configuration (the configured project set) describes its matrix, and the policy (the declared test controls) records how diagnostic and retry choices are set. The workflow invocation provides the automation edge when it is present (the represented workflow). Within that shape, the cases such as the named interaction scenarios are useful as coverage intent. Reading the surfaces together gives a maintainer a practical way to review drift: compare the command, configuration, policy, and scenario boundary before asking what an actual execution established.

## Cross-platform details

The platform detail starts with the configured project surfaces `chromium`, `firefox`, `mobile-chromium`, and `webkit`. Those names give maintainers a concrete comparison set for the section: they show where the suite is arranged to look, while remaining configuration rather than evidence that any environment completed successfully. The local policy values screenshot=only-on-failure show which controls travel with that configuration. Comparing those values with the named project surfaces makes drift visible as a repository review question instead of a runtime conclusion. The useful question is therefore whether the checked-in declarations remain synchronized across their named surfaces. Any behavior beyond those declarations stays outside the article until execution evidence supports it. This also gives maintenance a concrete rule: review a changed project declaration together with its related policy and invocation instead of assuming that the listed environments behave identically during ordinary source review.

## Accessibility and interaction

The interaction boundary is anchored in static cases such as “Dropdown supports keyboard navigation, focus restore, portal, outside click, and positioning” and “Modal renders in a portal, traps focus, and closes with Escape”. Those titles tell a reviewer which interaction intent to inspect, while stopping short of a claim that focus, state, keyboard, or accessibility behavior passed at runtime. This keeps the section focused on the relationship between named intent and the source surfaces that organize it, rather than on unsupported behavior claims. Separating interaction intent from execution outcome gives the reader a concrete next question: which implementation or run evidence should be inspected before treating the declared boundary as a supported result?

## Failure modes and edge cases

Failure-oriented review begins with what the suite records around an interaction case. The explicit policy values—the configured retry and diagnostic controls—show which controls are part of the declaration, while the scenario names make the intended edge cases concrete (“Select keeps focus on the trigger, handles keyboard dismissal, portals content, and uses its mobile sheet” and “Tooltip opens from focus, closes on tab movement, renders through a portal, and stays within the viewport”). The command and project surfaces keep those cases attached to a selectable test shape rather than to prose alone. That gives a reviewer a bounded checklist for future edits: preserve the policy, keep the project selection synchronized, and inspect the named scenario when its interaction contract changes. The facts still stop before execution: a declaration can make a failure mode inspectable without claiming that the failure was observed or that the case passed.

## Integration and enforcement

Integration is visible at the point where automation reaches the focused suite. The workflow records `test:e2e:web:visual` and `test:e2e:web`, which can be compared with the package entrypoint the focused command. The configured projects and policies then explain the shape of what that entrypoint selects. This makes CI wiring a reviewable relationship rather than a claim inferred from a workflow filename. The static cases remain a separate layer: they show the interaction intentions attached to the suite, while the workflow shows how the suite is invoked. Together those declarations tell a maintainer where to compare configuration drift, but they do not replace an authoritative execution result.

## Maintenance implications

The maintenance task is keeping the command, project selection, policy, and scenario boundary synchronized. A change to the focused entrypoint should be checked against the configured projects and declared policies; a workflow change should be compared with the automation invocation. The named cases (the represented scenarios) provide a second review surface for deciding whether the intended interaction coverage is still described. This is a useful maintenance discipline because each artifact answers a different question and no filename has to carry the whole story. Keep the explanation tied to those explicit relationships, and leave runtime conclusions to the execution evidence that can support them.

## Current limitations

The boundary is part of the design: configuration and static declarations describe the connected test configuration, but they do not establish that an execution passed or that an unlisted interaction behaves the same way. The recorded project list and policy values remain useful because they show the intended test surface and its explicit controls recorded by the policy. The workflow and command facts show how that surface is selected for automation, while the scenario names show which interaction intents were made visible. A reader can use those relationships to decide what to inspect next without turning repository arrangement into a runtime guarantee.
